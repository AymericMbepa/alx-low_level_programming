#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char ch1, ch2 = 'a', 'A';
while (ch1 <= 'z', ch2 <= 'Z')
{
putchar(ch1);
putchar(ch2);
ch1++;
ch2++;
}
return (0);
}
