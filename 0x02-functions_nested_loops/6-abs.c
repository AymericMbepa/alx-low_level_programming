#include "main.h"

/**
 * _abs - compute the absolute value of an integer
 * Return: Absolute value of an integer
 */
int _abs(int)
{
int value = _abs();
if (_abs() < 0)
{
value = -_abs();
}
return (value);
}
