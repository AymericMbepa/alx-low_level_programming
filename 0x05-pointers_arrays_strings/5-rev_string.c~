#include "main.h"

/**
 * rev_string - reverse string
 *@s: a string
 */
void rev_string(char *s)
{
	int lenght = 0, j = 0;

	while (s[lenght] != '\0')
		lenght++;
	while (lenght >= 0)
	{
		s[j++] = s[lenght--];
	}
}
